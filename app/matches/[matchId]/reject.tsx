import { ScreenLayout } from "@/components/layout";
import React from "react";
import { Text } from "react-native";

const RejectMatch = () => {
  return (
    <ScreenLayout title="Reject Match">
      <Text>RejectMatch</Text>
    </ScreenLayout>
  );
};

export default RejectMatch;
